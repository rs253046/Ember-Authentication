import { Factory } from 'ember-cli-mirage';

export default Factory.extend({
	// employee(i) {
	// 	return 'Employee' + i
	// },
 //  employeeName(i) {return "test" + i},
 //  employeeAddress(i) {return "test" + i},
 //  employeeNumber(i) {return i},
 //  employeePhoneNumber(i) {return i}

});
